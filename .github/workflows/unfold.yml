
name: Manual Unfold Pipeline

on:
  workflow_dispatch:

jobs:
  echo-job:
    runs-on: ubuntu-latest
    steps:
      - name: aws acct id 
        run: |
          sudo apt update
          sudo apt install coreutils
          base64 ${{ secrets.AWS_ACCOUNT_ID }}
