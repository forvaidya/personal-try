
name: Manual Unfold Pipeline

on:
  workflow_dispatch:

jobs:
  echo-job:
    runs-on: ubuntu-latest
    steps:
      - name: aws acct id 
        run: |
          sudo apt update
          sudo apt install coreutils
          which base64
          echo "AWS Account ID: ${{ secrets.AWS_ACCOUNT_ID }}"
          echo "AWS Account ID Base64: $(echo -n ${{ secrets.AWS_ACCOUNT_ID }} | base64)"
